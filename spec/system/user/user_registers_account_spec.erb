require 'rails_helper'

describe 'Usuário visita tela inicial' do
    it 'e clica em cadastra-se' do
        
        visit root_path
        within('nav') do
            click_on 'Cadastrar-se'
        end

        expect(page).to have_content 1° Quarto:
    
    end