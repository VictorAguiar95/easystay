<!DOCTYPE html>
<html>
  <head>
    <title>EasystayApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header>
      <h1>EasyStay</h1>
      <nav>
        <%= link_to 'Inicio', root_path %> |
        <%= link_to 'Anuncie Conosco', new_guesthouse_path %> |
        <%= link_to 'Agende sua viagem', new_guesthouse_path %> |
        <%= link_to 'Suporte', new_guesthouse_path %> |
        <%= link_to 'Cadastrar-se', new_user_registration_path %> |
        <% if user_signed_in?%>
          <%= current_user.email %> |
          <%= button_to 'Sair', destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to 'Entrar', new_user_session_path %> |
        <% end %>
      </nav>
    </header>
    <main>
      <div>
        <%= alert %>
        <%= notice %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
